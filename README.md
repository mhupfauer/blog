# hupfauer.one

Hugo + PaperMod, deployed to a VPS via cron-pulled rebuild.

## New post

    hugo new posts/<slug>.md

Edit, set `draft: false`, commit, push. Live within ~2 minutes.

## Local preview

    hugo server -D
